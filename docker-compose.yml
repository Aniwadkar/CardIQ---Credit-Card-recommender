services:
  cardiq-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cardiq-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      USE_MOCK_MODE: "true"
    restart: unless-stopped
